apiVersion: v1
kind: Service
metadata:
  name: paymentservice
  namespace: hipster-shop
spec:
  type: ClusterIP
  selector:
    app: paymentservice
  ports:
  - port: 50051
    targetPort: 50051
    protocol: TCP
    name: grpc
