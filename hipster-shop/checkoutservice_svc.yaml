apiVersion: v1
kind: Service
metadata:
  name: checkoutservice
  namespace: hipster-shop
spec:
  type: ClusterIP
  selector:
    app: checkoutservice
  ports:
  - port: 5050
    targetPort: 5050
    protocol: TCP
    name: grpc
