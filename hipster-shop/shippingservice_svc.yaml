apiVersion: v1
kind: Service
metadata:
  name: shippingservice
  namespace: hipster-shop
spec:
  type: ClusterIP
  selector:
    app: shippingservice
  ports:
  - port: 50051
    targetPort: 50051
    protocol: TCP
    name: grpc
