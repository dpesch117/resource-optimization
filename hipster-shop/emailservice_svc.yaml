apiVersion: v1
kind: Service
metadata:
  name: emailservice
  namespace: hipster-shop
spec:
  type: ClusterIP
  selector:
    app: emailservice
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: grpc
