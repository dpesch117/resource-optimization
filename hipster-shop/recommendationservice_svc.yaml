apiVersion: v1
kind: Service
metadata:
  name: recommendationservice
  namespace: hipster-shop
spec:
  type: ClusterIP
  selector:
    app: recommendationservice
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: grpc
