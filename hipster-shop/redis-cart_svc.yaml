apiVersion: v1
kind: Service
metadata:
  name: redis-cart
  namespace: hipster-shop
spec:
  type: ClusterIP
  selector:
    app: redis-cart
  ports:
  - port: 6379
    targetPort: 6379
    protocol: TCP
    name: redis
