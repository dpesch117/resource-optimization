apiVersion: v1
kind: Service
metadata:
  name: adservice
  namespace: hipster-shop
spec:
  type: ClusterIP
  selector:
    app: adservice
  ports:
  - port: 9555
    targetPort: 9555
    protocol: TCP
    name: grpc
